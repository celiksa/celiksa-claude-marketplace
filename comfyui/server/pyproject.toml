[project]
name = "comfyui-mcp-server"
version = "0.1.0"
description = "MCP server wrapping ComfyUI's REST API for Claude Code"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0",
    "httpx>=0.27",
    "websockets>=12.0",
]
